# Search hidden files if they're not ignored by VCS etc
--hidden

# Ignore the .git directory
--iglob=!.git

--iglob=!__fixtures__

# Only use case-sensitivity if input is not all lowercase
--smart-case
